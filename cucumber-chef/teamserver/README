Welcome to the teamserver suite of cucumber-chef tests

Your general workflow will be to write cucumber features that describe the intended behaviour of 'teamserver'.  You will then write failing tests by writing steps to test the behaviour, expressing them as cucumer scenarios. 

You will then want to create a 'teamserver' cookbook, and write recipes that make your tests pass.  Upload your cookbook to the Opscode Platform, and then run the tests with: 

Then run your tests with:

cucumber-chef test teamserver

For an example feature, take a look at ./teamserver/features/example.feature

For an example step definition, look at ./teamserver/features/example.feature

